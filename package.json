{
  "name": "quarto-lock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/quarto-lock.test.mjs",
    "preview:locked": "node scripts/preview-locked.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
